{
  "mcpVersion": "1.0",
  "name": "mitmproxy-mcp",
  "version": "0.6.1",
  "description": "Advanced network interception and debugging for AI agents.",
  "capabilities": {
    "tools": true,
    "resources": false
  },
  "appliesTo": ["local"],
  "auth": {
    "type": "none",
    "note": "Local stdio only. The MCP transport is stdio and the proxy binds loopback by default. Exposing this server over a network transport requires adding authentication and TLS; do not publish it as-is."
  }
}
